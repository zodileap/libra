
# zodileap-agen TODO

## 0. 产品与架构原则

- [ ] 明确平台定位：平台是主入口，智能体是可拆分、可独立售卖的模块。
- [ ] 明确模块边界：平台模块（用户、订阅、支付、企业管理）与智能体模块（代码、三维）解耦。
- [ ] 明确激活码策略：每个智能体可通过激活码独立激活。
- [ ] 明确 Core 定位：沉淀通用能力（流程管理、激活码鉴权、大模型调用网关）。
- [ ] 输出系统总图：Web、Desktop、API、DAO、Core、Sandbox、LLM 网关、支付系统关系图。

## 1. 仓库与工程初始化

- [ ] 建立项目目录骨架（web/desktop/services/core 等）。
- [ ] 建立模块化规范：智能体模块必须可独立打包、独立发布、独立授权。
- [ ] 配置统一环境变量规范（本地/测试/生产）。
- [ ] 配置基础 CI（检查、构建、测试）。
- [ ] 输出开发启动文档与模块开发指南。

## 2. 平台基础能力

- [ ] 用户基础能力：注册、登录、找回、账号信息管理。
- [ ] 用户授权能力：用户可被授权使用指定智能体。
- [ ] 左侧菜单 + 右侧内容区整体布局落地（Web 与 Desktop 风格一致）。
- [ ] 智能体入口管理：平台内展示可用智能体列表与状态。

## 3. 商业化与企业能力

- [ ] 订阅体系：全家桶订阅（订阅后可用全部智能体）。
- [ ] 单独购买体系：按智能体单独购买与授权。
- [ ] 激活码体系：按智能体生成、分发、核销、失效、续期。
- [ ] 支付与订单：下单、支付回调、订单状态同步。
- [ ] 企业账户体系：企业组织、成员、角色、权限、配额。
- [ ] 企业授权策略：企业统一购买后给成员分配智能体使用权限。

## 4. Core 通用能力（Rust）

- [ ] 流程管理引擎：任务流编排、状态机、重试策略。
- [ ] 激活码鉴定：校验规则、签名、有效期、设备绑定策略。
- [ ] 大模型调用网关：统一模型接入、限流、重试、审计。
- [ ] 通用协议定义：给 Web/Desktop/Backend 的统一接口契约。
- [ ] SDK/绑定层：提供后端与客户端可复用调用方式。

## 5. 智能体一：代码智能体

### 5.1 基础交互

- [ ] 构建类 Codex/Claude 的对话与任务界面。
- [ ] 支持代码生成、文件编辑、命令执行记录与结果回显。
- [ ] 支持会话历史与上下文管理。

### 5.2 预览能力（先支持 Web）

- [ ] 后端 Sandbox 服务：按会话启动隔离运行环境。
- [ ] 在 Sandbox 内运行前端项目并暴露预览地址。
- [ ] Web 端接入实时预览面板（仅 Web 项目）。
- [ ] 会话结束后的资源清理与回收策略。

### 5.3 代码一致性约束能力

- [ ] 建立“框架/组件/代码模块”资产管理（表单 + 列表 + 编辑）。
- [ ] 支持录入开源 UI 库（名称、Git 地址、版本、备注）。
- [ ] 支持上传自定义组件（名称、源码、说明、版本）。
- [ ] 支持录入代码模块模板（模块名、用途、依赖、源码）。
- [ ] 在生成代码时可选择并强制约束只使用已选资产。
- [ ] 建立资产审核与可用状态管理（草稿/启用/禁用）。

## 6. 智能体二：三维模型智能体

### 6.1 Web 端能力

- [ ] 支持三维模型生成任务提交与状态跟踪。
- [ ] 支持模型结果展示（Web 预览）。

### 6.2 Desktop 端能力

- [ ] 设计 DCC 连接层（Desktop 与本机软件通信）。
- [ ] 对接 Blender（导入、执行、导出基础流程）。
- [ ] 对接 ZBrush（导入、执行、导出基础流程）。
- [ ] 定义桌面端与 DCC 的任务协议、错误码与回调机制。

## 7. 数据与服务层

- [ ] services 下创建 `entity` 与 `api` 子目录。
- [ ] Entity（DAO）：按 schema 方式建模，执行代码生成脚本，产出实体与基础能力。
- [ ] API：toolkit 初始化后接入 Entity 生成代码，再补齐业务逻辑。
- [ ] 设计核心数据模型：用户、企业、智能体、订阅、订单、激活码、资产库、会话、任务、预览实例。

## 8. 安全与稳定性

- [ ] 权限模型：个人/企业/管理员多角色访问控制。
- [ ] Sandbox 安全：网络隔离、资源限制、文件隔离、超时回收。
- [ ] 审计与日志：关键操作留痕（支付、授权、激活、执行）。
- [ ] 监控告警：任务失败率、模型调用失败率、Sandbox 资源异常。

## 9. 发布与售卖准备

- [ ] 智能体模块打包规范：平台内置与独立插件两种发布形态。
- [ ] License/激活校验接入发布流程。
- [ ] 定义独立售卖交付物（安装包、插件包、授权文件、文档）。
- [ ] 验证“脱离平台单独使用”场景的最小可运行能力。

## 10. 当前阶段优先级（建议）

- [ ] P0：仓库骨架 + 用户基础 + 权限模型 + 激活码最小闭环。
- [ ] P0：代码智能体最小可用（对话、生成、Web 预览）。
- [ ] P1：资产约束能力（框架/组件/模块管理 + 生成约束）。
- [ ] P1：订阅/单买/企业管理。
- [ ] P2：三维智能体 Web 展示 + Desktop DCC 对接（Blender、ZBrush）。

## 11. 待你确认

- [ ] Sandbox 技术方案（容器方案、隔离级别、是否一会话一容器）。
- [ ] 支付渠道优先级与首发地区。
- [ ] 激活码与订阅关系（是否允许叠加、互斥、转移设备）。
- [ ] 三维智能体首发格式范围（如 glb/fbx/obj）。
- [ ] Web 与 Desktop 的首发功能差异清单。
