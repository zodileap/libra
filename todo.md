# zodileap-agen TODO

## P0（当前迭代，最高优先级）模型智能体：先实现第 3 步（MCP → Blender）

### 目标
- [x] 用户可直接对“当前已打开的 Blender 场景”执行导出（通过 Blender Bridge）。
- [ ] 用户可导入已有模型后，通过自然语言持续迭代修改。
- [ ] 暂不接入 ZBrush，先聚焦 Blender。

### 交互与流程
- [x] Desktop 端增加“模型编辑会话”入口（不依赖前两步生图/图生模）。
- [ ] 支持会话内连续指令（多轮编辑），可查看每一步执行记录。
- [ ] 支持撤销/重试最近一步操作（至少提供会话级回退能力）。

### MCP（Blender 工具）能力清单（首批）
- [ ] 场景与对象：列出对象、选择对象、重命名、层级整理。
- [ ] 变换：对齐原点、统一尺度、旋转方向标准化（坐标系约定）。
- [ ] 几何编辑：加厚、倒角、镜像、阵列、布尔（并/差/交）。
- [ ] 网格优化：自动平滑、Weighted Normal、必要时 Decimate。
- [ ] 材质与贴图：材质槽整理、贴图路径检查、打包。
- [x] 导出：按项目规范导出 GLB（命名、目录、单位、轴向统一）。
- [x] 当前会话桥接：Desktop -> Tauri -> MCP -> Blender Bridge -> 导出。

### 安全与可控
- [ ] 指令白名单：仅允许可控的 Blender 操作集合。
- [ ] 参数边界：面数、缩放、布尔次数、导出路径等设置硬限制。
- [ ] 执行超时与中断：单步超时可取消，避免卡死。
- [ ] 失败可解释：错误提示包含“失败步骤 + 原因 + 下一步建议”。

### 数据与日志
- [ ] 记录每次 MCP 调用：入参、耗时、结果摘要、错误信息。
- [ ] 会话资产记录：输入模型、输出模型、版本号、导出文件路径。
- [ ] 为后续“工作流编排”预留统一事件模型（step started/finished/failed）。

---

## P1 模型智能体：完整工作流（你定义的 0→1→2→2.5→3）

### 全流程能力
- [ ] 0. 输入：文本提示词 + 可选参考图/风格图。
- [ ] 1. 生图：生成 1~4 张候选图（支持持续调整）。在这之前需要先判断（是否添加了生图的AI key，比如Gemini ）
- [ ] 1.5 结构化描述：输出主体/材质/必须保留特征的结构化约束。
- [ ] 2. Meshy 图生 3D：生成初始 GLB。
- [ ] 2.5 Meshy 兜底细化：Remesh + Retexture（优先保证可编辑与材质一致）。
- [ ] 3. Gemini+MCP(Blender) 可解释细化：自然语言驱动高级修改并导出。

### 可从任意步骤进入（不是固定流水线）
- [ ] 支持“直接上传图片 → 图生模 → Blender 优化”。
- [ ] 支持“直接进入 Blender 当前模型 → MCP 优化”。
- [ ] 支持“从历史某一步继续”（如从 2.5 跳回 3）。

---

## P2 工作流系统（后续功能：可自定义）

- [ ] 工作流节点模型：输入/生图/图生模/细化/导出等节点标准化。
- [ ] 工作流编排：节点可插拔、可跳步、可重排、可复用模板。
- [ ] 用户自定义工作流：保存、复制、分享、版本管理。
- [ ] 节点级参数配置：模型选择、采样参数、质量/耗时策略。
- [ ] 失败恢复策略：节点重试、替代节点、人工接管。

---
