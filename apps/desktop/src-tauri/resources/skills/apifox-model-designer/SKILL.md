---
name: apifox-model-designer
description: 定义 API 实体、请求模型、响应模型，并约束同步步骤。
---
# 交互契约设计

## 何时使用
- 需要先统一前后端字段、接口契约或 Mock 场景。
- 需要将契约同步到 Apifox 或 MCP 工具链。

## 执行要求
1. 先定义实体结构与关键字段。
2. 再定义请求模型、响应模型与异常场景。
3. 涉及 Apifox 或 MCP 时，先校验当前工具是否可用，再执行同步。

## 输出格式
- API 实体
- 请求模型
- 响应模型
- Mock 场景与同步结果
