// Code generated by taurus_go/entity, DO NOT EDIT.

package runtime

import (
	"context"
	"time"

	"git.zodileap.com/entity/runtime_v1/agent_session"
	"git.zodileap.com/entity/runtime_v1/internal"
	"github.com/zodileap/taurus_go/entity"
	"github.com/zodileap/taurus_go/entity/dialect"
	"github.com/zodileap/taurus_go/entity/entitysql"
)

// agentsessionentityBuilder is a builder for the AgentSessionEntity entity.
//
// The builder is used to create, update, and delete AgentSessionEntity entities.
type agentsessionentityBuilder struct {
	config    *agentsessionentityConfig
	tracker   entity.Tracker
	Id        agent_session.PredId        // Id 主键Id
	UserId    agent_session.PredUserId    // UserId 用户Id
	AgentCode agent_session.PredAgentCode // AgentCode 智能体编码
	Status    agent_session.PredStatus    // Status 状态
	CreatedAt agent_session.PredCreatedAt // CreatedAt 创建数据时间
	LastAt    agent_session.PredLastAt    // LastAt 更新数据时间
	DeletedAt agent_session.PredDeletedAt // DeletedAt 删除数据时间（逻辑删除）
	// ById configures the query to sort results based on the 'id' field of the entity.
	// Sorting entities in ascending order by default.
	ById agent_session.ById
	// ByUserId configures the query to sort results based on the 'user_id' field of the entity.
	// Sorting entities in ascending order by default.
	ByUserId agent_session.ByUserId
	// ByAgentCode configures the query to sort results based on the 'agent_code' field of the entity.
	// Sorting entities in ascending order by default.
	ByAgentCode agent_session.ByAgentCode
	// ByStatus configures the query to sort results based on the 'status' field of the entity.
	// Sorting entities in ascending order by default.
	ByStatus agent_session.ByStatus
	// ByCreatedAt configures the query to sort results based on the 'created_at' field of the entity.
	// Sorting entities in ascending order by default.
	ByCreatedAt agent_session.ByCreatedAt
	// ByLastAt configures the query to sort results based on the 'last_at' field of the entity.
	// Sorting entities in ascending order by default.
	ByLastAt agent_session.ByLastAt
	// ByDeletedAt configures the query to sort results based on the 'deleted_at' field of the entity.
	// Sorting entities in ascending order by default.
	ByDeletedAt agent_session.ByDeletedAt
}

// newAgentSessionEntityBuilder creates a new AgentSessionEntityBuilder .
func newAgentSessionEntityBuilder(c *agentsessionentityConfig, t entity.Tracker) *agentsessionentityBuilder {
	return &agentsessionentityBuilder{
		config:  c,
		tracker: t,
	}
}

// Create creates a new UserEntity，and add it to the tracker.
// Required parameters are fields that have no default value but are required,
// and options are fields that can be left empty by calling WithFieldName.
func (b *agentsessionentityBuilder) Create(user_id string, agent_code string, options ...func(*AgentSessionEntity)) (*AgentSessionEntity, error) {
	e := b.config.New()
	switch t := e.(type) {
	case *AgentSessionEntity:
		return t.create(user_id, agent_code, options...)
	default:
		return nil, entity.Err_0100030006
	}
}

func (b *agentsessionentityBuilder) Remove(e *AgentSessionEntity) error {
	if e.config.Mutation == nil {
		return nil
	}
	return e.remove()
}

// First returns the first AgentSessionEntity.
func (s *agentsessionentityBuilder) First(ctx context.Context) (*AgentSessionEntity, error) {
	query := s.initQuery()
	return query.First(ctx)
}

func (s *agentsessionentityBuilder) ToList(ctx context.Context) ([]*AgentSessionEntity, error) {
	query := s.initQuery()
	return query.ToList(ctx)
}

func (s *agentsessionentityBuilder) Include(rels ...agentsessionentityRel) *AgentSessionEntityQuery {
	query := s.initQuery()
	return query.Include(rels...)
}

func (s *agentsessionentityBuilder) Order(o ...agent_session.OrderTerm) *AgentSessionEntityQuery {
	query := s.initQuery()
	return query.Order(o...)
}

func (s *agentsessionentityBuilder) Where(conditions ...entitysql.PredicateFunc) *AgentSessionEntityQuery {
	query := s.initQuery()
	return query.Where(conditions...)
}

// WithId sets the "id" field of the AgentSessionEntity.
func (s *agentsessionentityBuilder) WithId(id int64) func(*AgentSessionEntity) {
	return func(e *AgentSessionEntity) {
		e.Id.Set(id)
	}
}

// WithStatus sets the "status" field of the AgentSessionEntity.
func (s *agentsessionentityBuilder) WithStatus(status int16) func(*AgentSessionEntity) {
	return func(e *AgentSessionEntity) {
		e.Status.Set(status)
	}
}

// WithCreatedAt sets the "created_at" field of the AgentSessionEntity.
func (s *agentsessionentityBuilder) WithCreatedAt(createdat time.Time) func(*AgentSessionEntity) {
	return func(e *AgentSessionEntity) {
		e.CreatedAt.Set(createdat)
	}
}

// WithLastAt sets the "last_at" field of the AgentSessionEntity.
func (s *agentsessionentityBuilder) WithLastAt(lastat time.Time) func(*AgentSessionEntity) {
	return func(e *AgentSessionEntity) {
		e.LastAt.Set(lastat)
	}
}

// WithDeletedAt sets the "deleted_at" field of the AgentSessionEntity.
func (s *agentsessionentityBuilder) WithDeletedAt(deletedat time.Time) func(*AgentSessionEntity) {
	return func(e *AgentSessionEntity) {
		e.DeletedAt.Set(deletedat)
	}
}

// Exec executes all the agentsessionentityMutations for the AgentSessionEntity.
func (s *agentsessionentityBuilder) Exec(ctx context.Context, tx dialect.Tx) error {
	if len(s.config.agentsessionentityMutations.Addeds) > 0 {
		e := s.config.agentsessionentityMutations.Get(entity.Added)
		n := newAgentSessionEntityCreate(s.config.Dialect, e...)
		if err := n.create(ctx, tx); err != nil {
			return err
		}
	}
	if len(s.config.agentsessionentityMutations.Modifieds) > 0 {
		e := s.config.agentsessionentityMutations.Get(entity.Modified)
		n := newAgentSessionEntityUpdate(s.config.Dialect, e...)
		if err := n.update(ctx, tx); err != nil {
			return err
		}
	}
	if len(s.config.agentsessionentityMutations.Deleteds) > 0 {
		e := s.config.agentsessionentityMutations.Get(entity.Deleted)
		n := newAgentSessionEntityDelete(s.config.Dialect, e...)
		if err := n.delete(ctx, tx); err != nil {
			return err
		}
	}
	return nil
}

func (s *agentsessionentityBuilder) initQuery() *AgentSessionEntityQuery {
	return newAgentSessionEntityQuery(s.config.Dialect, s.tracker, s.config.agentsessionentityMutations)
}

// agentsessionentityMutations is a collection of AgentSessionEntity mutation.
type agentsessionentityMutations struct {
	Detacheds  map[string]*AgentSessionEntity
	Unchangeds map[string]*AgentSessionEntity
	Deleteds   map[string]*AgentSessionEntity
	Modifieds  map[string]*AgentSessionEntity
	Addeds     map[string]*AgentSessionEntity
}

// newAgentSessionEntityMutations creates a new mutations.
func newAgentSessionEntityMutations() *agentsessionentityMutations {
	return &agentsessionentityMutations{
		Detacheds:  make(map[string]*AgentSessionEntity),
		Unchangeds: make(map[string]*AgentSessionEntity),
		Deleteds:   make(map[string]*AgentSessionEntity),
		Modifieds:  make(map[string]*AgentSessionEntity),
		Addeds:     make(map[string]*AgentSessionEntity),
	}
}

// Get returns all the AgentSessionEntity in the specified state.
func (ms *agentsessionentityMutations) Get(state entity.EntityState) []*AgentSessionEntity {
	switch state {
	case entity.Detached:
		s := make([]*AgentSessionEntity, 0, len(ms.Detacheds))
		for _, m := range ms.Detacheds {
			s = append(s, m)
		}
		return s
	case entity.Unchanged:
		s := make([]*AgentSessionEntity, 0, len(ms.Unchangeds))
		for _, m := range ms.Unchangeds {
			s = append(s, m)
		}
		return s
	case entity.Deleted:
		s := make([]*AgentSessionEntity, 0, len(ms.Deleteds))
		for _, m := range ms.Deleteds {
			s = append(s, m)
		}
		return s
	case entity.Modified:
		s := make([]*AgentSessionEntity, 0, len(ms.Modifieds))
		for _, m := range ms.Modifieds {
			s = append(s, m)
		}
		return s
	case entity.Added:
		s := make([]*AgentSessionEntity, 0, len(ms.Addeds))
		for _, m := range ms.Addeds {
			s = append(s, m)
		}
		return s
	}
	return nil
}

// SetEntityState sets the state of the entity.
func (ms *agentsessionentityMutations) SetEntityState(e *AgentSessionEntity, state entity.EntityState) error {
	m := e.config.Mutation
	ms.set(e, state)
	if err := internal.SetEntityState(m, state); err != nil {
		return err
	}
	return nil
}

// ChangeEntityState attempts to set the desired entity state,
// but will not do so if the conditions are not met.
func (ms *agentsessionentityMutations) ChangeEntityState(m *entity.Mutation, state entity.EntityState) {
	e := ms.getEntity(m)
	ms.set(e, state)
	if err := internal.SetEntityState(m, state); err != nil {
		return
	}
}

// getEntity returns the entity in the specified state.
func (ms *agentsessionentityMutations) getEntity(m *entity.Mutation) *AgentSessionEntity {
	key := m.Key()
	switch m.State() {
	case entity.Detached:
		return ms.Detacheds[key]
	case entity.Unchanged:
		return ms.Unchangeds[key]
	case entity.Deleted:
		return ms.Deleteds[key]
	case entity.Modified:
		return ms.Modifieds[key]
	case entity.Added:
		return ms.Addeds[key]
	}
	return nil
}

// Set sets the entity in the specified state.
func (ms *agentsessionentityMutations) set(e *AgentSessionEntity, state entity.EntityState) {
	m := e.config.Mutation
	key := m.Key()
	switch m.State() {
	case entity.Detached:
		delete(ms.Detacheds, key)
	case entity.Unchanged:
		delete(ms.Unchangeds, key)
	case entity.Deleted:
		delete(ms.Deleteds, key)
	case entity.Modified:
		delete(ms.Modifieds, key)
	case entity.Added:
		delete(ms.Addeds, key)
	}
	if state >= 0 {
		switch state {
		case entity.Detached:
			ms.Detacheds[key] = e
		case entity.Unchanged:
			ms.Unchangeds[key] = e
		case entity.Deleted:
			ms.Deleteds[key] = e
		case entity.Modified:
			ms.Modifieds[key] = e
		case entity.Added:
			ms.Addeds[key] = e
		}
	}
}
