// Code generated by taurus_go/entity, DO NOT EDIT.

package account

import (
	"context"
	"time"

	"git.zodileap.com/entity/account_v1/internal"
	"git.zodileap.com/entity/account_v1/permission_grant"
	"github.com/zodileap/taurus_go/entity"
	"github.com/zodileap/taurus_go/entity/dialect"
	"github.com/zodileap/taurus_go/entity/entitysql"
)

// permissiongrantentityBuilder is a builder for the PermissionGrantEntity entity.
//
// The builder is used to create, update, and delete PermissionGrantEntity entities.
type permissiongrantentityBuilder struct {
	config         *permissiongrantentityConfig
	tracker        entity.Tracker
	Id             permission_grant.PredId             // Id 主键Id
	ActorUserId    permission_grant.PredActorUserId    // ActorUserId 操作人用户Id
	TargetUserId   permission_grant.PredTargetUserId   // TargetUserId 目标用户Id
	TargetUserName permission_grant.PredTargetUserName // TargetUserName 目标用户名称
	PermissionCode permission_grant.PredPermissionCode // PermissionCode 权限编码
	ResourceType   permission_grant.PredResourceType   // ResourceType 资源类型
	ResourceName   permission_grant.PredResourceName   // ResourceName 资源名称
	GrantedBy      permission_grant.PredGrantedBy      // GrantedBy 授权人用户Id
	Status         permission_grant.PredStatus         // Status 状态
	ExpiresAt      permission_grant.PredExpiresAt      // ExpiresAt 过期时间
	CreatedAt      permission_grant.PredCreatedAt      // CreatedAt 创建数据时间
	LastAt         permission_grant.PredLastAt         // LastAt 更新数据时间
	DeletedAt      permission_grant.PredDeletedAt      // DeletedAt 删除数据时间（逻辑删除）
	// ById configures the query to sort results based on the 'id' field of the entity.
	// Sorting entities in ascending order by default.
	ById permission_grant.ById
	// ByActorUserId configures the query to sort results based on the 'actor_user_id' field of the entity.
	// Sorting entities in ascending order by default.
	ByActorUserId permission_grant.ByActorUserId
	// ByTargetUserId configures the query to sort results based on the 'target_user_id' field of the entity.
	// Sorting entities in ascending order by default.
	ByTargetUserId permission_grant.ByTargetUserId
	// ByTargetUserName configures the query to sort results based on the 'target_user_name' field of the entity.
	// Sorting entities in ascending order by default.
	ByTargetUserName permission_grant.ByTargetUserName
	// ByPermissionCode configures the query to sort results based on the 'permission_code' field of the entity.
	// Sorting entities in ascending order by default.
	ByPermissionCode permission_grant.ByPermissionCode
	// ByResourceType configures the query to sort results based on the 'resource_type' field of the entity.
	// Sorting entities in ascending order by default.
	ByResourceType permission_grant.ByResourceType
	// ByResourceName configures the query to sort results based on the 'resource_name' field of the entity.
	// Sorting entities in ascending order by default.
	ByResourceName permission_grant.ByResourceName
	// ByGrantedBy configures the query to sort results based on the 'granted_by' field of the entity.
	// Sorting entities in ascending order by default.
	ByGrantedBy permission_grant.ByGrantedBy
	// ByStatus configures the query to sort results based on the 'status' field of the entity.
	// Sorting entities in ascending order by default.
	ByStatus permission_grant.ByStatus
	// ByExpiresAt configures the query to sort results based on the 'expires_at' field of the entity.
	// Sorting entities in ascending order by default.
	ByExpiresAt permission_grant.ByExpiresAt
	// ByCreatedAt configures the query to sort results based on the 'created_at' field of the entity.
	// Sorting entities in ascending order by default.
	ByCreatedAt permission_grant.ByCreatedAt
	// ByLastAt configures the query to sort results based on the 'last_at' field of the entity.
	// Sorting entities in ascending order by default.
	ByLastAt permission_grant.ByLastAt
	// ByDeletedAt configures the query to sort results based on the 'deleted_at' field of the entity.
	// Sorting entities in ascending order by default.
	ByDeletedAt permission_grant.ByDeletedAt
}

// newPermissionGrantEntityBuilder creates a new PermissionGrantEntityBuilder .
func newPermissionGrantEntityBuilder(c *permissiongrantentityConfig, t entity.Tracker) *permissiongrantentityBuilder {
	return &permissiongrantentityBuilder{
		config:  c,
		tracker: t,
	}
}

// Create creates a new UserEntity，and add it to the tracker.
// Required parameters are fields that have no default value but are required,
// and options are fields that can be left empty by calling WithFieldName.
func (b *permissiongrantentityBuilder) Create(actor_user_id string, target_user_id string, target_user_name string, permission_code string, resource_type string, resource_name string, options ...func(*PermissionGrantEntity)) (*PermissionGrantEntity, error) {
	e := b.config.New()
	switch t := e.(type) {
	case *PermissionGrantEntity:
		return t.create(actor_user_id, target_user_id, target_user_name, permission_code, resource_type, resource_name, options...)
	default:
		return nil, entity.Err_0100030006
	}
}

func (b *permissiongrantentityBuilder) Remove(e *PermissionGrantEntity) error {
	if e.config.Mutation == nil {
		return nil
	}
	return e.remove()
}

// First returns the first PermissionGrantEntity.
func (s *permissiongrantentityBuilder) First(ctx context.Context) (*PermissionGrantEntity, error) {
	query := s.initQuery()
	return query.First(ctx)
}

func (s *permissiongrantentityBuilder) ToList(ctx context.Context) ([]*PermissionGrantEntity, error) {
	query := s.initQuery()
	return query.ToList(ctx)
}

func (s *permissiongrantentityBuilder) Include(rels ...permissiongrantentityRel) *PermissionGrantEntityQuery {
	query := s.initQuery()
	return query.Include(rels...)
}

func (s *permissiongrantentityBuilder) Order(o ...permission_grant.OrderTerm) *PermissionGrantEntityQuery {
	query := s.initQuery()
	return query.Order(o...)
}

func (s *permissiongrantentityBuilder) Where(conditions ...entitysql.PredicateFunc) *PermissionGrantEntityQuery {
	query := s.initQuery()
	return query.Where(conditions...)
}

// WithId sets the "id" field of the PermissionGrantEntity.
func (s *permissiongrantentityBuilder) WithId(id int64) func(*PermissionGrantEntity) {
	return func(e *PermissionGrantEntity) {
		e.Id.Set(id)
	}
}

// WithGrantedBy sets the "granted_by" field of the PermissionGrantEntity.
func (s *permissiongrantentityBuilder) WithGrantedBy(grantedby string) func(*PermissionGrantEntity) {
	return func(e *PermissionGrantEntity) {
		e.GrantedBy.Set(grantedby)
	}
}

// WithStatus sets the "status" field of the PermissionGrantEntity.
func (s *permissiongrantentityBuilder) WithStatus(status int16) func(*PermissionGrantEntity) {
	return func(e *PermissionGrantEntity) {
		e.Status.Set(status)
	}
}

// WithExpiresAt sets the "expires_at" field of the PermissionGrantEntity.
func (s *permissiongrantentityBuilder) WithExpiresAt(expiresat string) func(*PermissionGrantEntity) {
	return func(e *PermissionGrantEntity) {
		e.ExpiresAt.Set(expiresat)
	}
}

// WithCreatedAt sets the "created_at" field of the PermissionGrantEntity.
func (s *permissiongrantentityBuilder) WithCreatedAt(createdat time.Time) func(*PermissionGrantEntity) {
	return func(e *PermissionGrantEntity) {
		e.CreatedAt.Set(createdat)
	}
}

// WithLastAt sets the "last_at" field of the PermissionGrantEntity.
func (s *permissiongrantentityBuilder) WithLastAt(lastat time.Time) func(*PermissionGrantEntity) {
	return func(e *PermissionGrantEntity) {
		e.LastAt.Set(lastat)
	}
}

// WithDeletedAt sets the "deleted_at" field of the PermissionGrantEntity.
func (s *permissiongrantentityBuilder) WithDeletedAt(deletedat time.Time) func(*PermissionGrantEntity) {
	return func(e *PermissionGrantEntity) {
		e.DeletedAt.Set(deletedat)
	}
}

// Exec executes all the permissiongrantentityMutations for the PermissionGrantEntity.
func (s *permissiongrantentityBuilder) Exec(ctx context.Context, tx dialect.Tx) error {
	if len(s.config.permissiongrantentityMutations.Addeds) > 0 {
		e := s.config.permissiongrantentityMutations.Get(entity.Added)
		n := newPermissionGrantEntityCreate(s.config.Dialect, e...)
		if err := n.create(ctx, tx); err != nil {
			return err
		}
	}
	if len(s.config.permissiongrantentityMutations.Modifieds) > 0 {
		e := s.config.permissiongrantentityMutations.Get(entity.Modified)
		n := newPermissionGrantEntityUpdate(s.config.Dialect, e...)
		if err := n.update(ctx, tx); err != nil {
			return err
		}
	}
	if len(s.config.permissiongrantentityMutations.Deleteds) > 0 {
		e := s.config.permissiongrantentityMutations.Get(entity.Deleted)
		n := newPermissionGrantEntityDelete(s.config.Dialect, e...)
		if err := n.delete(ctx, tx); err != nil {
			return err
		}
	}
	return nil
}

func (s *permissiongrantentityBuilder) initQuery() *PermissionGrantEntityQuery {
	return newPermissionGrantEntityQuery(s.config.Dialect, s.tracker, s.config.permissiongrantentityMutations)
}

// permissiongrantentityMutations is a collection of PermissionGrantEntity mutation.
type permissiongrantentityMutations struct {
	Detacheds  map[string]*PermissionGrantEntity
	Unchangeds map[string]*PermissionGrantEntity
	Deleteds   map[string]*PermissionGrantEntity
	Modifieds  map[string]*PermissionGrantEntity
	Addeds     map[string]*PermissionGrantEntity
}

// newPermissionGrantEntityMutations creates a new mutations.
func newPermissionGrantEntityMutations() *permissiongrantentityMutations {
	return &permissiongrantentityMutations{
		Detacheds:  make(map[string]*PermissionGrantEntity),
		Unchangeds: make(map[string]*PermissionGrantEntity),
		Deleteds:   make(map[string]*PermissionGrantEntity),
		Modifieds:  make(map[string]*PermissionGrantEntity),
		Addeds:     make(map[string]*PermissionGrantEntity),
	}
}

// Get returns all the PermissionGrantEntity in the specified state.
func (ms *permissiongrantentityMutations) Get(state entity.EntityState) []*PermissionGrantEntity {
	switch state {
	case entity.Detached:
		s := make([]*PermissionGrantEntity, 0, len(ms.Detacheds))
		for _, m := range ms.Detacheds {
			s = append(s, m)
		}
		return s
	case entity.Unchanged:
		s := make([]*PermissionGrantEntity, 0, len(ms.Unchangeds))
		for _, m := range ms.Unchangeds {
			s = append(s, m)
		}
		return s
	case entity.Deleted:
		s := make([]*PermissionGrantEntity, 0, len(ms.Deleteds))
		for _, m := range ms.Deleteds {
			s = append(s, m)
		}
		return s
	case entity.Modified:
		s := make([]*PermissionGrantEntity, 0, len(ms.Modifieds))
		for _, m := range ms.Modifieds {
			s = append(s, m)
		}
		return s
	case entity.Added:
		s := make([]*PermissionGrantEntity, 0, len(ms.Addeds))
		for _, m := range ms.Addeds {
			s = append(s, m)
		}
		return s
	}
	return nil
}

// SetEntityState sets the state of the entity.
func (ms *permissiongrantentityMutations) SetEntityState(e *PermissionGrantEntity, state entity.EntityState) error {
	m := e.config.Mutation
	ms.set(e, state)
	if err := internal.SetEntityState(m, state); err != nil {
		return err
	}
	return nil
}

// ChangeEntityState attempts to set the desired entity state,
// but will not do so if the conditions are not met.
func (ms *permissiongrantentityMutations) ChangeEntityState(m *entity.Mutation, state entity.EntityState) {
	e := ms.getEntity(m)
	ms.set(e, state)
	if err := internal.SetEntityState(m, state); err != nil {
		return
	}
}

// getEntity returns the entity in the specified state.
func (ms *permissiongrantentityMutations) getEntity(m *entity.Mutation) *PermissionGrantEntity {
	key := m.Key()
	switch m.State() {
	case entity.Detached:
		return ms.Detacheds[key]
	case entity.Unchanged:
		return ms.Unchangeds[key]
	case entity.Deleted:
		return ms.Deleteds[key]
	case entity.Modified:
		return ms.Modifieds[key]
	case entity.Added:
		return ms.Addeds[key]
	}
	return nil
}

// Set sets the entity in the specified state.
func (ms *permissiongrantentityMutations) set(e *PermissionGrantEntity, state entity.EntityState) {
	m := e.config.Mutation
	key := m.Key()
	switch m.State() {
	case entity.Detached:
		delete(ms.Detacheds, key)
	case entity.Unchanged:
		delete(ms.Unchangeds, key)
	case entity.Deleted:
		delete(ms.Deleteds, key)
	case entity.Modified:
		delete(ms.Modifieds, key)
	case entity.Added:
		delete(ms.Addeds, key)
	}
	if state >= 0 {
		switch state {
		case entity.Detached:
			ms.Detacheds[key] = e
		case entity.Unchanged:
			ms.Unchangeds[key] = e
		case entity.Deleted:
			ms.Deleteds[key] = e
		case entity.Modified:
			ms.Modifieds[key] = e
		case entity.Added:
			ms.Addeds[key] = e
		}
	}
}
